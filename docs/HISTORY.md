# History

- `roobli/roob-note-site` (Quartz 5 + Typora Claude-like theme) was an earlier “public notes” attempt at `note.roobli.org`.
- Decision 2026-09-11: notes ≠ public. Public surface is this Astro repo (`presence`) at `www.roobli.org`.
- Theme tokens (`src/styles/claude-like-tokens.css`) were ported from that Quartz customization; the Quartz engine itself is not used here.
