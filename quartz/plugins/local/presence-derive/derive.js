/**
 * Page data stored as file.data.presence: kind, figures, sections,
 * figureOffsets, words, readingMinutes and relation. Both passes are empty
 * until the derivation is written; renderPage falls back to the slug for kind.
 */

export function markdownPlugins(_ctx) {
  return []
}

export function htmlPlugins(_ctx) {
  return []
}
