/**
 * Home index: the derived Writing and Works rows on the homepage and the
 * folder pages. Not built yet, so the component renders nothing.
 */
export const HomeIndex = () => {
  const Component = () => null
  return Component
}
